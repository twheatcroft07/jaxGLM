[pytest]
# Default run = the fast CPU tier (skip slow/GPU-scale tests). Run everything with: pytest -m ""
addopts = -m "not slow" -q
markers =
    slow: long-running / GPU-scale test (skipped by default; run with -m slow)
python_files = test_*.py
python_functions = test_*
